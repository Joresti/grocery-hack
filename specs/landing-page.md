Feature: Landing Page Data Loading

Scenario: User opens the app
Given the user is authenticated
When the app loads the landing page
Then a single API call to GET /landing should return all page data
And the page should render from this single data object


Feature: Landing Page Savings Summary

Scenario: User views savings summary
Given the user opens the application
Then the application logo should be visible on the left or centered
And a savings summary should appear on the right side containing:
  | label             | value  |
  | Savings This Week | $xx.xx |
  | YTD Savings       | $xx.xx |

And the following primary buttons should appear below the header:
  | button name            |
  | Go to My Smart List    |
  | Optimize My Smart List |


Feature: Absurd Deal Alert Banner

Scenario: Absurd deal is available
Given the user is on the landing page
And an absurd deal exists in watchlist_alerts with price_tier "luxury"
Then an eye-catching banner should appear below the primary buttons
And the banner should highlight the deal with pulsing animation

Scenario: No absurd deal available
Given the user is on the landing page
And no absurd deal exists
Then the absurd deal banner should not be displayed


Feature: Your Recipes on Sale

Scenario: User recipe matches current deals
Given the user has saved recipes
And one or more recipes match this week's deals (from recipe_alerts)
Then a "Your Recipes on Sale" section should appear below the absurd deal alert
And each recipe alert card should display:
  | element                        |
  | recipe name                    |
  | number of ingredients on sale  |
  | estimated cost this week       |
  | estimated regular cost         |
  | savings amount                 |

Scenario: No user recipes match deals
Given the user has saved recipes
And none match this week's deals
Then the "Your Recipes on Sale" section should not appear


Feature: My Liked Meals (preview)

Scenario: User has liked meals
Given the user is on the landing page
And the user has previously liked one or more meals
Then a section titled "My Liked Meals" should appear
And up to 6 recently liked meals should be shown as compact cards
And each card should display:
  | element                                |
  | meal name                              |
  | meal image                             |
  | "X ingredients on sale" badge (if > 0) |
  | estimated cost (if available)          |

Scenario: Liked meal has ingredients on sale
Given a liked meal has 3 ingredients currently on sale
Then the meal card should display a teal badge "3 ingredients on sale this week"
And the badge should list the ingredient names on tap

Scenario: Liked meal has no ingredients on sale
Given a liked meal has 0 ingredients currently on sale
Then no sale badge should appear on that card
And the card should still be tappable to view the full recipe

Scenario: User taps a liked meal card
Given the user taps a liked meal card
Then the RecipeModal should open with deal-enriched ingredient pricing

Scenario: User taps "View All"
Given the user has more than 6 liked meals
Then a "View All" link should appear at the bottom of the section
When the user taps "View All"
Then a full-screen LikedMealsModal should open showing all liked meals
And each meal should show the deal context badge

Scenario: User has no liked meals
Given the user has not liked any meals
Then the "My Liked Meals" section should not appear

Scenario: Anonymous user
Given the user is not logged in
Then the "My Liked Meals" section should not appear


Feature: Dream Meal Matching

Scenario: Display dream meal matches
Given the user is on the landing page
Then a section titled "Match Your Dream Meals" with a heart icon should be visible

And each meal card should display:
  | element                                          |
  | meal image                                       |
  | recipe name                                      |
  | price per serving                                |
  | attribution line (if shared by a user)           |

Scenario: Meal card shows attribution for community recipe
Given a meal in the swipe deck was shared by a user
Then the meal card should show a small colored initials circle and "Shared by {name}"
And the attribution line should appear below the deal badge in textMuted style

Scenario: Meal card shows no attribution for system meal
Given a meal in the swipe deck was generated by the system
Then no attribution line should appear on the card

Scenario: User selects a dream meal
Given the user sees a meal card
When the user clicks the meal card
Then a full screen modal should open
And the previous page should be hidden


Feature: Meal Modal Navigation

Scenario: Swipe through meal options
Given the user is viewing a meal in the modal
When the user swipes left
Then the next meal should appear

When the user swipes right
Then the previous meal should appear


Feature: Cook For / Cook With Requests

Scenario: User asks someone to cook the meal
Given the user is viewing a meal in the modal
When the user clicks "Cook this for me?"
Then a contact modal should appear
And the user should be able to enter:
  | field             |
  | recipient contact |
  | optional date     |
  | optional time     |

When the user submits the request
Then an email or SMS should be sent to the recipient

Scenario: User offers to cook a meal
Given the user is viewing a meal in the modal
When the user clicks "I'll make this for you!"
Then a contact modal should appear
And the user should be able to enter:
  | field             |
  | recipient contact |
  | optional date     |
  | optional time     |

Scenario: Recipient accepts cooking invitation
Given the recipient accepts the invitation
Then a calendar event should be created
And it should be added to both users' calendars


Feature: Store Meal Deal List

Scenario: Display store and meal information
Given the user scrolls to the store deals section
Then each store listing should show:
  | field          |
  | store name     |
  | store location |

And the store location should link to Google Maps

Scenario: View meal details
Given a meal title is displayed under a store
When the user clicks the meal title
Then a modal should open showing the meal card and full details

Scenario: Display shopping list under each store
Given the user views a store section
Then below the meals, the ingredient-level shopping list should appear
And each item should show quantity, sale price, and regular price


Feature: Meal Ingredient Pricing

Scenario: Ingredient is on sale
Given an ingredient is on sale
Then the original price should be displayed with a strikethrough
And the discounted price should be displayed next to it

Scenario: Ingredient is not on sale
Given an ingredient is not on sale
Then the ingredient should be displayed with a different style
And the ingredient should be marked as a pantry or non-sale item


Feature: Notable Deals Section

Scenario: Notable deals exist
Given the user scrolls to the bottom of the landing page
And notable deals are available
Then a section titled "Notable Deals" should be displayed
And up to 10 notable deals should be shown

Scenario: Few notable deals exist
Given fewer than 10 deals qualify as notable
Then only those deals should be displayed


Feature: Notable Deal Card Content

Scenario: User views a notable deal
Given a notable deal is displayed
Then the deal card should include:
  | element        |
  | product image  |
  | product name   |
  | store name     |
  | store location |
  | original price |
  | sale price     |

And the original price should appear with a strikethrough
And the sale price should appear beside it
And a heart button should allow the user to add the deal to their watchlist


Feature: Notable Deal Navigation

Scenario: User clicks store location
Given the user sees a notable deal
When the user clicks the store location
Then Google Maps should open with the store address


Feature: Feeling Lucky

Scenario: User taps Feeling Lucky
Given the user is on the landing page
When the user taps the "Feeling Lucky" button
Then a full-screen modal should open

Scenario: First time use
Given the Feeling Lucky modal is open
And no household names have been entered
Then the modal should prompt "Who's cooking this week?"
And show a text field to add names one at a time

Scenario: Return use
Given the Feeling Lucky modal is open
And household names exist
Then the names should appear pre-filled
And each name should have an X to delete it
And the user can add new names

Scenario: Spin the wheel
Given household names have been entered
When the user taps "Spin!"
Then a slot-machine animation should roll through meals and names
And land on a random combination
And confetti should appear
And the selected meal should come from the user's liked pool only


Feature: Open Smart List Optimizer

Scenario: User opens optimizer
Given the user is on the landing page
When the user clicks "Optimize My Smart List"
Then an optimization form should appear
And the user should be able to configure optimization preferences


Feature: Select Location for Optimization

Scenario: User enters postal code
Given the optimization form is open
When the user enters a postal code
Then the system should use that postal code to determine nearby stores

Scenario: User selects current location
Given the optimization form is open
When the user clicks "Use Current Location"
Then the system should request permission to access the user's location

Scenario: User grants location permission
Given the system requested location access
When the user grants permission
Then the user's current location should be used for optimization


Feature: Select Stores for Optimization

Scenario: Optimization form loads
Given the user opens the "Optimize My Smart List" form
Then the system should fetch the default list of stores from the backend
And the stores should appear as selectable options

Scenario: User selects preferred stores
Given the optimization form displays available stores
When the user selects one or more stores
Then those stores should be included in the optimization

Scenario: User deselects a store
Given the optimization form displays available stores
When the user deselects a store
Then the optimization should ignore that store


Feature: Store Travel Limit

Scenario: User selects single store optimization
Given the optimization form is open
When the user selects "1 Store"
Then the optimization should prioritize minimizing cost within a single store

Scenario: User selects two store optimization
Given the optimization form is open
When the user selects "2 Stores"
Then the optimization should allow items to be sourced from up to two stores


Feature: Request Flyer Source

Scenario: User submits flyer request
Given the user is on the "Optimize My Smart List" page
When the user enters a flyer URL
And submits the request
Then the flyer request should be recorded
And the flyer should appear in the flyer list
And the status should be "Pending Review"


Feature: Display Flyer Requests

Scenario: Display flyer requests
Given the user has submitted flyer requests
Then each flyer request should appear in the flyer list

And each entry should display:
  | element        |
  | flyer url      |
  | status         |
  | review message |


Feature: Flyer Request Lifecycle

Scenario: Flyer awaiting review
Given a flyer request has been submitted
And the request has not been reviewed
Then the status should display "Pending Review"
And the flyer entry should appear in a neutral style

Scenario: Flyer request approved
Given a flyer request has been approved
Then the flyer entry should display a checkmark
And the flyer should be included in smart list optimization
And the status should display "Approved"

Scenario: Flyer request rejected
Given a flyer request has been rejected
Then the flyer entry should appear greyed out
And the status should display "Rejected"
And the user should see the reason for rejection


Feature: Share Plan

Scenario: User shares full plan
Given the user is on the landing page
When the user taps "Share this plan"
Then a contact modal should appear
And the user should enter recipient name and email or phone
When the user submits
Then the recipient should receive a link to the full plan via email or SMS
And the recipient does not need a GroceryHack account to view it


Feature: Share Important Items

Scenario: User shares important items
Given the user has an active important items list
When the user shares it with a recipient
Then the recipient should receive a view-only link
And if the recipient wants to edit, they must create an account
And editing creates a clone of the list linked to the recipient's account


Current GroceryHack Landing Page Structure:

  Header
    Logo (left)
    Savings Summary (right): This Week + YTD

  Primary Actions
    Go to My Smart List
    Optimize My Smart List

  Absurd Deal Alert (conditional — watchlist luxury alerts)

  Your Recipes on Sale (conditional — user recipes matching deals)

  My Liked Meals (conditional — preview of up to 6, with deal badges)

  Dream Meal Matching (swipe cards)

  Store Meal Deal List + Shopping List (combined under each store)

  Feeling Lucky

  Notable Deals (up to 10)
